<div class="row">
  {{#each items}}
    <div class="column">
      <span class="color" style="background-color: {{color}}"></span>
      <span class="value">{{value}}</span>
    </div>
  {{/each}}
</div>
