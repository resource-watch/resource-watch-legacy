<div class="row">
  {{#each items}}
    <div class="column">
      <span class="color" style="background-color: {{color}}; {{#if icon}}background-image: asset-url('{{icon}}'){{/if}}"></span>
      <span class="value">{{name}}</span>
    </div>
  {{/each}}
</div>
