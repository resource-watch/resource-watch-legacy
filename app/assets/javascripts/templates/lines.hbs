<div class="row">
  {{#each items}}
    <div class="column">
      <span class="color {{#if lineType}}-{{lineType}}{{/if}}" style="border-color: {{color}}"></span>
      <span class="value">{{name}}</span>
    </div>
  {{/each}}
</div>
