<% breadcrumb :root %>
<div class="rw-l-banner">
  <%= render "shared/featured_insight_banner" %>
</div>
<div class="row">
  <div class="col -xs-12">
    <section class="rw-l-section">
      <header class="header -col">
        <h2 class="title">Latest Insights</h2>
        <p>Read the latest stories, findings and analysis from our community</p>
      </header>

      <%= render "shared/insights" %>

      <button class="action -gray">
        <%= link_to page_path('insights'), class:'link' do %>
          <%= t('insights.explore') %>
        <% end %>
      </button>
    </section>
  </div>
</div>

<%= render "shared/planet_pulse" %>

<div class="row">
  <div class="col -xs-12">
    <section class="rw-l-section">
      <%= render "shared/explore_dashboard" %>
    </section>
  </div>
</div>

<!-- Provisional images -->
<div class="row">
  <div class="col -xs-12">
    <section class="rw-l-section">
      <header class="header -col">
        <h2 class="title">Get involved</h2>
      </header>

      <%= image_tag "get_involved.png", class: 'image' %>
    </section>
  </div>
</div>

<div class="row -last">
  <div class="col -xs-12">
    <section class="rw-l-section">
      <header class="header -col">
        <h2 class="title">Social Media</h2>
      </header>

      <%= image_tag "social.png", class: 'image' %>
    </section>
  </div>
</div>

<%= image_tag "apps.png", class: 'image' %>
